{
  "name": "raycart",
  "version": "0.0.1",
  "description": "Wasm host exposing the full raylib API — compile games with wasi-sdk, run on native or web without recompile",
  "type": "module",
  "exports": "./host/web/index.js",
  "files": [
    "host/web/index.js",
    "host/web/emfs.js",
    "host/web/raylib_em.mjs"
  ],
  "scripts": {
    "codegen": "node scripts/gen_cart_c_header.js",
    "build:host": "node scripts/build_host_web.js",
    "build:carts": "node scripts/build_cart.js carts/*",
    "build": "npm run codegen && npm run build:host && npm run build:carts",
    "start": "npm run build:carts && npx -y live-server --port=3000 host/web",
    "test": "node --test",
    "fmt": "npx -y prettier -w ."
  },
  "dependencies": {
    "@easywasm/wasi": "^0.0.8",
    "fflate": "^0.8.2"
  }
}
